build:
  # Install dependencies
  - pip install -r requirements.txt
  - python manage.py collectstatic --noinput

start:
  # Start Gunicorn server
  - gunicorn hospital_project.wsgi:application --bind 0.0.0.0:10000
